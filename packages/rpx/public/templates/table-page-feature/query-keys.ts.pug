import type { #{pcName}PageDto } from '@raipiot-2f/api'

export const #{constName}S_QK = '#{ccName}s'

export const #{constName}_QK = '#{ccName}'

export const #{ccName}sQK = (params?: #{pcName}PageDto) => [#{constName}S_QK, params]

export const #{ccName}QK = (id?: string) => [#{constName}_QK, { id }]

