import type { #{pcName}Dto, #{pcName}SubmitDto } from '@raipiot-2f/api'

export interface #{pcName}SearchFormModel extends Pick<#{pcName}PageDto, 'code' | '#{ccName}Value'> {}

export interface #{pcName}SubmitFormModel extends Omit<#{pcName}SubmitDto, 'isSealed'> {
  isSealed?: boolean
}
