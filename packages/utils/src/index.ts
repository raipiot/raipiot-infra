export const add = (value: number, increment: number) => value + increment

export const subtract = (value: number, decrement: number) => value - decrement

export const multiply = (value: number, multiplier: number) => value * multiplier

export const divide = (value: number, divisor: number) => value / divisor
